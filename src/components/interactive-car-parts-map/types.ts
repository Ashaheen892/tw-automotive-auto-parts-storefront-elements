export type DetailMode = 'inline' | 'sheet';

export interface CarPart {
  id: string;
  name: string;
  x: number;
  y: number;
  title: string;
  desc: string;
  icon: string;
  link: string;
  image: string;
  tip: string;
}
